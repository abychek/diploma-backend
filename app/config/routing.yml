projects:
    resource: "@ProjectsBundle/Controller/"
    type:     annotation
    prefix:   /

staff:
    resource: "@StaffBundle/Controller/"
    type:     annotation
    prefix:   /api/staff

app:
    resource: '@AppBundle/Controller/'
    type: annotation
